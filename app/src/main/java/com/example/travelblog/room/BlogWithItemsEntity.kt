package com.example.travelblog.room
/*

import androidx.room.Embedded
import androidx.room.Entity
import androidx.room.PrimaryKey
import androidx.room.Relation

//Relation between BlogEntity and BlogItemEntity class
@Entity(tableName = "blogWithItems")
data class BlogWithItemsEntity(
    @Embedded val blog: BlogEntity,
    @Relation(
        parentColumn = "blogId", //blog id column in BlogEntity
        entityColumn = "blogIdRef" //blog id column in BlogItemEntity
    )
    val blogItemEntities: List<BlogItemEntity>  //blog with its items
    )

//Note: Embedded fields can also include other embedded fields.



*/




